#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_build:
	go build -ldflags="-s -w" -o git-mini-commit .

override_dh_auto_install:
	install -D -m 755 git-mini-commit $(CURDIR)/debian/git-mini-commit/usr/bin/git-mini-commit
